# Combine functions into transform

## If there are several calculations performed on the same data, then we create a single transformation that immediately adds the results of these calculations to the data.

### The logic is being consolidated into a single function within a single context. Instead of 10 different functions that operate on a single data field

## Example

<img width="715" height="780" alt="image" src="https://github.com/user-attachments/assets/5f2802ba-0116-4dc3-bf36-1d3f7fcc83b4" />
